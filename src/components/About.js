import styles from './About.css';
import React from 'react';

const About = () => (
  <div className={styles.root}>
    <h2>About us</h2>
    <p>We are an insecure security agency.</p>
  </div>
);

export default About;
